# Reproducible runtime stack. Update requirements-lock.txt deliberately and
# validate the complete Torch/PennyLane/XGBoost suite before changing pins.
-r requirements-lock.txt

# Optional autoencoder path in Notebook 4 / run_pipeline.py --method autoencoder
# tensorflow is intentionally not part of the supported audit-tested runtime.
