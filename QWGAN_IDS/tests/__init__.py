"""Test package root.

Both ``tests/fr3`` and ``tests/fr4`` import the shared NSL-KDD fixture from
here, so this package must stay importable: run the suite with
``python -m unittest discover -s tests -t .``.
"""
